fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
supabase==2.10.0
httpx==0.27.2
groq==0.13.0
gitpython==3.1.43
sentence-transformers==3.3.1
# LangChain stack intentionally excluded in Phase 1 backend runtime.
# Direct Groq SDK calls are used in planner/scoring and avoid NumPy<2 constraints on Python 3.13.
# spacy intentionally excluded — PII masking uses regex only in Phase 1
PyGithub==2.5.0
pydantic==2.10.3
python-multipart==0.0.20
